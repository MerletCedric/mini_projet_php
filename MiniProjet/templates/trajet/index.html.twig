{% extends 'base.html.twig' %}

{% block title %}Trajet index{% endblock %}

{% block body %}
    <h1>Trajet index</h1>

     <h1>Liste des trajets</h1>
    <ul class="list-group">
        {% for trajet in trajets %}
            <li class="list-group-item">
                <div>
                    <a href="{{path('app_trajet_show', {id:trajet.id})}}">
                        <strong>{{trajet.conducteur}}</strong> 
                        {{trajet.dateDepart|date('d/m/Y')}} 
                        {{trajet.heureDepart|date('h:m:s')}}
                    </a> 
                </div>    
                {{trajet.villeDepart}} > {{trajet.villeArrivee}}
                {{trajet.nbPlacesVoiture}}
            </li>
            {% else %}
            <li>
                Aucune données trouvées.
            </li>
        {% endfor %}
    </ul>

    <a href="{{ path('app_trajet_new') }}">Create new</a>
{% endblock %}
